import { PrismaClient } from '@prisma/client'
const prisma = new PrismaClient()

export module flock {

    export const showFlock = async() => 
    {
        
    }

    export const indexFlocks = async () =>
    {
        
    }

    export const createFlock = async () => 
    {
        
    }

    export const deleteFlock = async () =>
    {
        
    }
}