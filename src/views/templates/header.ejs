<head>
    <meta charset='utf-8'>
    <title>ElementKit</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel="stylesheet" type="text/css" href="../public/style/main.css">
</head>
<header class="header-row">
    <a href='/'>
        <h1> 
            ElementKit - <%= title %>
        </h1>
    </a>

    <div class="navigation-bar row">
        <a <% if (title == 'Dashboard') { %> class="selected" <% } %> href='/'>
            dashboard
        </a>
        <a <% if (title == 'user') { %> class="selected" <% } %> href='/user-index'>
            users
        </a>
        <a <% if (title == 'Auth') { %> class="selected" <% } %> href='/auth'>
            login
        </a>

        <a <% if (title == 'Auth') { %> class="selected" <% } %> href='/profile'>
            profile
        </a>

        <a href='/logout'>logout</a>
    </div> 
</header>